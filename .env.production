# Netlify Environment Variables
# Add these to your Netlify dashboard under Site settings > Environment variables

# Neon Database URL (get this from your Neon dashboard)
VITE_DATABASE_URL=postgresql://[user]:[password]@[host]/[database]?sslmode=require

# Google OAuth (for production)
VITE_GOOGLE_CLIENT_ID=your-production-google-client-id

# App Configuration
VITE_APP_NAME=HireFlow
VITE_APP_URL=https://your-netlify-domain.netlify.app
